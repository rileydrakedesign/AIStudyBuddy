# Node.js Backend Environment Variables
# Copy this file to .env and fill in your actual values

# Environment
NODE_ENV=development

# Server Configuration
PORT=3000

# Security
JWT_SECRET=your-local-jwt-secret-change-this
COOKIE_SECRET=your-local-cookie-secret-change-this

# Database
MONGO_CONNECTION_STRING=mongodb+srv://username:password@cluster.mongodb.net/study_buddy_demo?retryWrites=true&w=majority

# Frontend Configuration
CLIENT_ORIGIN=http://localhost:5173

# Python AI Service
PYTHON_API_URL=http://localhost:8000

# AWS S3 Configuration
AWS_ACCESS_KEY=your-aws-access-key
AWS_SECRET=your-aws-secret-key
AWS_REGION=us-east-1
AWS_S3_BUCKET_NAME=your-bucket-name

# Email Service (Mailgun)
MAILGUN_API_KEY=
MAILGUN_DOMAIN=
MAILGUN_FROM=noreply@classchatai.com

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id

# Redis (Production)
REDIS_TLS_URL=rediss://default:password@redis-host:port

# Notes:
# - Use the same MongoDB, S3, and Redis as production for local development
# - Create a test user account for local testing (don't use production user data)
# - JWT_SECRET and COOKIE_SECRET can be different from production for local dev
# - CLIENT_ORIGIN must be http://localhost:5173 for Vite dev server
