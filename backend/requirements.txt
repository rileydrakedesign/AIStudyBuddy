# ───────────────────── core framework ─────────────────────
fastapi==0.115.4
uvicorn==0.23.2          # dev & local; gunicorn wraps it in prod
gunicorn==22.0.0         # prod ASGI HTTP server
pydantic==2.9.2
python-multipart==0.0.9  # FastAPI file-upload helper
python-dotenv==1.0.1     # loads .env in local dev

# ───────────────────── data & I/O ─────────────────────────
pymongo==4.8.0           # MongoDB Atlas
boto3==1.34.142          # AWS S3
PyPDF2==3.0.1            # PDF parser fallback
pymupdf==1.24.13         # main PDF → Markdown extractor

# ───────────────────── LLM / RAG stack ────────────────────
openai==1.54.0
langchain-openai==0.2.5
langchain-core==0.3.15
langchain-text-splitters==0.3.2
langchain-mongodb==0.2.0
langchain-experimental==0.3.3
semantic-router==0.1.8

# ───────────────────── utilities ──────────────────────────
loguru==0.7.3            # structured logging
boto3-stubs[essential]   # optional type hints; safe to drop
